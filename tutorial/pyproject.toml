[project]
name = "phpjoy-framework"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "py2neo",
    "networkx",
    "matplotlib",
    "ruamel.yaml",
    "Levenshtein",
    "coloredlogs"
]
